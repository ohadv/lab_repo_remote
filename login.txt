This file was created under login branch. The login brach created by $ git branch login

To swtich from  (master) branch to (login) branch, use checkout login

The file generated by $ touch login.txt.

The file edited by $ vim login.txt. The write quit exit Esc + :wq

To add this to commit stage => we usually use $ git add . 

Since $ git add . will add all files, it is imprtant to update .gitignore file, with

all files and directories to be excluded from the commit. 

Use restore to revert changes.

Use clear to clean the bash

(remove directory rm -r ... , make directory by mkdir ...)



